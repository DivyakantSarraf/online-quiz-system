{
  "name": "online-quiz-system",
  "version": "1.0.0",
  "description": "An online quiz system built with HTML, CSS, JS and Firebase",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^10.8.0"
  },
  "devDependencies": {
    "vite": "^5.1.0"
  }
}
